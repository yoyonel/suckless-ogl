{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build test_postprocess",
            "type": "shell",
            "command": "make",
            "args": [
                "test_postprocess"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Compiles the test_postprocess executable"
        }
    ]
}
